import {Response, Request} from "express";

export const uploadPlayBack = (req: Request, res: Response) => {

    // GET THE FILE
    res.send(req.files);

    //WRITE WATERMARK

    //SAVE TO DATABASE

    // RETURN

}